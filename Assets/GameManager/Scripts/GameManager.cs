using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void LoseCondition()
    {
        //Player doesn't achieve atleast one star in time..
        //if(Time <= 0 && points < "points for one star")
    }

    void WinCondition()
    {
        //Player achieves atleast one star in time..
        //if(Time <= 0 && points >= "points for one star")
    }
}
